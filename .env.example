# ============================================================
# OLLAMA (Modelo de Linguagem / IA)
# ============================================================
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:1.5b
OLLAMA_KEEP_ALIVE=30m

# ============================================================
# WHISPER (Transcrição de Voz para Texto)
# ============================================================
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
WHISPER_LANGUAGE=pt

# ============================================================
# DETECÇÃO DE PALAVRA-CHAVE (Wake Word)
# ============================================================
WAKE_WORD=jarvis
WAKE_THRESHOLD=0.40

# ============================================================
# CONFIGURAÇÕES DE ÁUDIO E MICROFONE
# ============================================================
# -1 = microfone padrão do Windows. Rode test_microfone.py para descobrir
# o índice de um dispositivo específico, se necessário.
AUDIO_DEVICE_INDEX=-1
AUDIO_SAMPLE_RATE=16000
SILENCE_THRESHOLD=60
# SILENCE_SECONDS: quanto tempo de silêncio esperar antes de considerar
# que você terminou de falar. Se costuma falar pausadamente (pausas entre
# palavras/frases), suba esse valor — senão o Jarvis corta o comando no
# meio e a transcrição sai errada/incompleta.
SILENCE_SECONDS=2.0
MAX_RECORD_SECONDS=12.0
MIN_SPEECH_SECONDS=0.35
PRE_ROLL_SECONDS=0.35
# Sobe/desce a sensibilidade do "isso é fala ou é silêncio?" (padrão 2.2x
# o ruído ambiente). Se estiver cortando fala baixinha, baixe pra ~1.6.
SILENCE_MULTIPLIER=2.2
SAVE_AUDIO=true

# ============================================================
# PIPER (Síntese de Voz / Resposta Falada)
# ============================================================
# Caminho para o executável bundled do projeto (models/piper/piper.exe).
# Use apenas "piper" se tiver instalado piper-tts globalmente no PATH.
PIPER_MODEL=models/piper/pt_BR-faber-medium.onnx
PIPER_EXECUTABLE=models/piper/piper.exe

# ============================================================
# LOGS E AVISOS
# ============================================================
HF_HUB_DISABLE_SYMLINKS_WARNING=1
PYTHONWARNINGS=ignore
